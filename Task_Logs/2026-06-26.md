# 2026-06-26 作物分類匹配優化與每日快取機制

## 任務概述
重構農產品交易行情 (FarmTransData) 與官方作物代碼目錄 (LC7YWlenhLuP) 的匹配邏輯，並更新作物目錄至完整 6 級資料結構。

## 完成內容

### 1. 完整 6 級 Crop Catalog 更新
- 新增 `scripts/update_catalog.cjs` — 自動抓取 LC7YWlenhLuP，將 4370 筆官方資料轉為包含完整 plv1~plv6 欄位的 `public/data/crop_catalog.json`
- 檔案大小 1550 KB（由原本只保留 4 欄，改為完整 6 級 14 個欄位）

### 2. 重構 `cropIndex.js`
- **重寫 `normalizeName`**：清除「進口」「本土」「其他」等後綴、括號、修正「小果」對照
- **新增 `splitMarketName`**：將行情名稱（如 `甘藍-改良尖`）拆解為 `主要品項 + 品種` 兩段
- **重寫 `buildCropIndex`**：建立 `cnameMap`（官方 CNAME 全名對照）與 `plv3Map`（三級分類主品名對照）兩張索引
- **重寫 `lookupCrop`**：四級匹配策略
  1. 完整名稱精確比對 CNAME
  2. 主品項比對 PLV3_NAME
  3. 北農別名表 (`MARKET_ALIAS`) 轉換後再比對（例：`包心白` → `結球白菜`，`皇宮菜` → `落葵`）
  4. 模糊搜尋 CNAME

### 3. 同步更新 `aggregation.js`
- 呼叫簽章更新為新版 `buildCropIndex(catalog)` 與 `lookupCrop(name, index)` 介面

### 4. 比對驗證 (`scripts/test_match.cjs`)
- 測試 12 筆行情品名，結果 **11/12 成功**
- 唯一失敗為 `其他`（行情 API 兜底通用品名，無需對應，為預期行為）

## 測試結果
```
結果：11 成功 / 1 失敗 / 共 12 筆
失敗: 其他 (預期行為，不予處理)
```

## 檔案變動清單
- [MODIFY] `src/utils/cropIndex.js` — 完整重構
- [MODIFY] `src/utils/aggregation.js` — 呼叫簽章更新
- [NEW]    `scripts/update_catalog.cjs` — 官方目錄更新指令碼
- [NEW]    `scripts/test_match.cjs` — 比對邏輯驗證指令碼
- [UPDATE] `public/data/crop_catalog.json` — 更新為 6 級完整資料

## 待後續跟進
- `MARKET_ALIAS` 別名表只收錄了確認的對應，後續實際跑完整行情後若有未對應品名，可持續補充

---

## 下午追加：Catalog 瘦身（移除花卉 + 稻米）

### 動機
使用者確認本 App 只關心「蔬菜 + 水果」，花卉與稻米為雜訊。

### 動作

#### A. Crop Catalog 端（已完成）
- `public/data/crop_catalog.json`：4370 → **1217 筆**（-3153）
  - 移除 `plv1='004' 花卉類` 3120 筆
  - 移除 `plv1='001' 稻米類` 32 筆 + 1 筆編碼損壞（「稻類」）
  - 檔案 1550 KB → **415 KB**
- `scripts/update_catalog.cjs`：新增 `EXCLUDE_PLV1 = ['001', '004']`，下次重抓自動過濾

#### B. Fetch 端（已完成）
- `src/services/api.js` `fetchPrices()`：抓回資料後立即 `r.種類代碼 !== 'N06'` 過濾花卉
- 同步修正 [api.js:56] 註解錯誤（原寫 001=果樹類、003=稻米類，應為 001=稻米、003=果樹）

#### C. 連環 Bug 修正（重要）
- [src/utils/aggregation.js:120] `category === 'fruit'` 原本錯誤過濾 `plv1 === '001'`，應為 `'003'`
  - 修正原因：原本因為稻米類還在 catalog，「水果類」分頁實際抓的是稻米（雖然沒價格所以顯示空）。砍完稻米後若不修此 bug，水果類將完全沒資料。

### 驗證
```
=== Catalog 現況 ===
['002 蔬菜類', 531]
['003 果樹類', 332]
['005 雜糧類', 56]
['006 特用作物類', 130]
['007 農產品加工類', 137]
['010 其他作物類', 31]
總筆數: 1217

=== test_match.cjs ===
9 成功 / 3 失敗
- 玫瑰-紫霧：花卉，預期失敗
- 玫瑰-山中傳奇：花卉，預期失敗
- 其他：空字串邊界，預期失敗
（對比昨日 11/12 通過——其中 2 個「玫瑰」通過是因為當時花卉還在 catalog，屬 dead 測項）
```

### 效能附帶效益
- `cropIndex.js` `lookupCrop` 第四級「模糊搜尋」原本需 O(N) 全掃 cnameMap，N 從 4370 → 1217 後每次 miss 約快 3.6x
- `fetchPrices()` 過濾 N06 後，前端 matching pipeline 處理量也會降低

### 檔案變動
- [MODIFY] `public/data/crop_catalog.json` — 4370 → 1217 筆
- [MODIFY] `src/services/api.js` — fetchPrices 加 N06 過濾、修正 PLV1 註解
- [MODIFY] `src/utils/aggregation.js` — fruit 條件 001 → 003
- [MODIFY] `scripts/update_catalog.cjs` — 加入 EXCLUDE_PLV1

### 待討論（未動工）
005 雜糧（玉米/落花生）、006 特用（茶/咖啡）、007 加工品、010 其他作物——是否也要砍？
- 已透過 matching 測試確認 `玉米`、`落花生` 兩個雜糧正常運作，**建議保留 005**
- 006/007/010 內是否有出現在行情 API 的品項未知，需先跑一次完整行情比對才能決定

---

## 下午追加 #2：Catalog 最終瘦身（全類別裁決）

### 決議
使用者確認全砍：006 特用 / 007 加工 / 010 其他。只保留 002 蔬菜 + 003 果樹 + 005 雜糧。

### 動作
- `public/data/crop_catalog.json`：1217 → **919 筆**（-298），415KB → **312KB**
- `scripts/update_catalog.cjs` `EXCLUDE_PLV1` 加入 `'006', '007', '010'`
- `docs/architecture_map.md`：更新筆數與狀態表

### 驗證
```
=== Catalog 現況 ===
['002 蔬菜類', 531]
['003 果樹類', 332]
['005 雜糧類', 56]
total: 919

=== test_match.cjs ===
9 成功 / 3 失敗（3 fail 全為預期：花卉 2 + 邊界 1）
```

### Catalog 演進總結
| 階段 | 筆數 | 檔案大小 |
|------|------|----------|
| 原始（4 欄） | — | 較小 |
| 完整 6 級 14 欄 | 4370 | 1.5 MB |
| 砍花卉+稻米 | 1217 | 415 KB |
| **最終（蔬菜+果樹+雜糧）** | **919** | **312 KB** |

---

## 下午追加 #3：4 個工作流問題處理

### 動機
走查 App.jsx → DetailModal 後，發現多項架構性缺陷。

### 問題與解法

#### 問題 1：30 天價格曲線 + 分頁處理
- **現況**：`fetchPrices()` 只抓近 4 天，DetailModal `slice(-7)` 永遠看不到 7 天
- **解法**：
  - `fetchPrices({ days = 30 })` 預設 30 天範圍
  - 切成不重疊的 7 天/段，5 段並行抓取
  - `Map` 去重（`日期|作物|市場` 三元組）避免接縫重複
  - `MAX_DAYS=30` 為硬上限，防止惡意呼叫

#### 問題 2：DetailModal cropName 嚴格相等
- **現況**：`甘藍-初秋` 與 `甘藍-改良尖` 視為兩個不同作物，點開 DetailModal 只看自己變體
- **解法**：
  - `aggregation.js` 在 crop 物件加 `mainName` 欄位（去掉變體後的主品項）
  - `DetailModal.jsx` 改用 `splitMarketName(r.作物名稱).main === mainName` 模糊比對
  - 同主品項的所有變體會彙整進趨勢圖

#### 問題 3：fetchPrices 沒有 timeout/retry
- **現況**：政府 API 偶爾慢/掛，會讓 spinner 永遠轉
- **解法**：
  - `AbortController` 15s timeout
  - 每段最多重試 1 次（MAX_RETRIES）
  - 失敗 console.warn，徹底失敗 console.error，仍降級回傳 `[]`

#### 問題 4：MARKET_ORDER 重複定義
- **現況**：`['台北二', '台中市', '高雄市']` 在 4 個檔案寫死
- **解法**：
  - `api.js` 改為 `export const MARKET_ORDER = Object.values(MARKET_IDS).map(m => m.name)` 推導
  - `DetailModal.jsx` 移除本地寫死，改 import
  - `ProductCard.jsx` 移除本地寫死，改 import

### 檔案變動
- [MODIFY] [src/services/api.js](../src/services/api.js) — 重寫 fetchPrices、統一 MARKET_ORDER、加 timeout/retry
- [MODIFY] [src/utils/aggregation.js](../src/utils/aggregation.js) — 加 mainName、清 CATEGORY_LABELS 死 import
- [MODIFY] [src/components/DetailModal/DetailModal.jsx](../src/components/DetailModal/DetailModal.jsx) — 30 天趨勢、mainName 比對、移除寫死 MARKET_ORDER
- [MODIFY] [src/components/ProductCard/ProductCard.jsx](../src/components/ProductCard/ProductCard.jsx) — 移除寫死 MARKET_ORDER
- [MODIFY] [src/App.jsx](../src/App.jsx) — 清 CATEGORY_LABELS 死 import

### 驗證
```
=== ESM export 對稱性 ===
api.js: CATEGORY_LABELS, MARKET_IDS, MARKET_ORDER, fetchPrices, loadCropCatalog
cropIndex.js: buildCropIndex, lookupCrop, normalizeName, splitMarketName
aggregation.js: aggregatePrices, filterCrops, isPeakSeason

所有 import 都對得上 export ✓
```

### ⚠️ 環境限制（無法在此跑 dev/build）
- 本機 Node.js v16.17.0 太舊，Vite 8 要求 ≥20.19
- npm install 已安裝 114 個套件，但 Vite 與 oxlint 啟動時 crash
- **無法在此環境跑 `npm run dev` 做瀏覽器實測**，需在升級 Node 後於本機驗證

### 待後續
- [ ] Node 升級後跑 `npm run dev` 瀏覽器驗證
- [ ] `vege_favorites` vs `vage_favorites` 文件不一致（main.md / useFavorites.js）
- [ ] `useFavorites.js` 的 localStorage 鍵改名或修文件

---

## 下午追加 #4：後端每日快取 + localStorage 鍵統一

### A. localStorage 鍵統一為 `vege_favorites`
- `src/hooks/useFavorites.js` 本來就是 `vege_favorites`（無需改）
- `main.md` 文件更新：`vage_favorites` → `vege_favorites`（修正 typo）
- `docs/architecture_map.md` 之前就寫對的（無需改）

### B. 後端每日快取機制

#### 設計
- 新增 `scripts/daily_cache.cjs` — Node 腳本，由排程觸發
- 產出 `public/data/prices_cache.json`
- 首次：抓近 30 天 → 過濾 → 去重 → 寫入
- 後續：讀舊 cache + 抓今日 → 過濾 → append → trim 到 30 天
- 前端 `fetchPrices()` 改為讀 cache，cache miss 時 fallback 即時抓 4 天

#### 過濾規則（與 src/services/api.js 對齊）
| 規則 | 值 |
|------|-----|
| 市場 | 只留 104 台北二 / 400 台中市 / 800 高雄市 |
| 種類代碼 | 只留 N04 蔬菜 / N05 水果 |
| 排除 | N06 花卉（雖 N04/N05 已涵蓋，但顯式排除保險） |

#### 排程觸發方式（使用者決定）
- cron (Linux/Mac)
- GitHub Actions
- Windows Task Scheduler
- 手動：`node scripts/daily_cache.cjs`

### 實測結果

#### 首次建構
```
首次建構：抓近 30 天行情（7 天/段並行）
日期分段: [[115.06.20,115.06.26], [115.06.13,115.06.19], ...]
原始筆數: 47248
過濾後筆數: 10361
去重後筆數: 10361
✓ 寫入 10361 筆到 public/data/prices_cache.json (3257.6 KB)
```

#### 增量更新（同日再跑一次）
```
增量更新：現有 cache 筆數 10361，上次更新 2026-06-26
今日原始筆數: 0       ← 政府 API 收盤前還沒資料
合併去重後筆數: 10361
trim 到 30 天後筆數: 10361
✓ 寫入 10361 筆
```

#### Cache 內容檢查
- 日期範圍：115.05.28 ~ 115.06.25（17 個交易日，週末/假日正常缺資料）
- 市場分布：台北二 4610 / 台中市 2991 / 高雄市 2760
- 種類分布：N04 蔬菜 7361 / N05 水果 3000
- 零 N06 花卉 ✓
- 零非 3 市場 ✓

### 效能數字
- 首次：5 段並行抓 30 天 → 47K 筆原始 → 過濾後 10K 筆 → 寫 3.26 MB
- 增量：1 段抓 1 天 → merge → trim
- 前端：App 啟動時 fetch ~3.26 MB JSON（4G 估 1-2 秒；可後續壓縮）

### 檔案變動
- [NEW] [scripts/daily_cache.cjs](../scripts/daily_cache.cjs) — 快取建構腳本
- [NEW] [public/data/prices_cache.json](../public/data/prices_cache.json) — 首次建構產出（10,361 筆）
- [MODIFY] [src/services/api.js](../src/services/api.js) — `fetchPrices()` 改讀 cache，`fetchPricesLive()` 為 fallback
- [MODIFY] [main.md](../main.md) — localStorage 鍵名修正

### ⚠️ 後續優化方向
- Cache 3.26 MB 偏大，未來可考慮：
  - 移除 `交易量` 等列表不需要的欄位（可能瘦到 ~1.5 MB）
  - 改用 IndexedDB 取代 JSON 整批讀取
- 目前 cache miss fallback 是抓 4 天，若使用者長期不更新資料會有資料過期風險
  - 可加 stale 提示：「行情資料已是 X 天前，請更新」


---

## 下午追加 #5：Cache 瘦身 + GitHub Pages 部署 + App 自更新

### A. Cache 瘦身

**問題**：原始 cache 3.26 MB 偏大，App 啟動下載 ~1-2 秒。

**解法**：移除不必要欄位 + 改用單行 JSON 寫入。
- 移除欄位：`交易量`（無用）、`種類代碼`（已過濾）、`市場代號`（市場名稱已涵蓋）
- 改用 `JSON.stringify(result)` 而非 `JSON.stringify(result, null, 2)`（省縮排空白）
- dedup key 從「市場代號」改為「市場名稱」（已同步 daily_cache.cjs 與 api.js）

**結果**：3.26 MB → **1.68 MB（省 48.5%）**

### B. GitHub Pages + App 自更新（依 Plan: github-pages-greedy-otter.md）

#### 使用者決策
- App 端持久化：**IndexedDB**（純 Web API）
- 啟動更新：**阻塞首屏**（先 fetch 遠端確認最新才渲染）
- 部署方式：**gh-pages 分支 + GitHub Actions**

#### 實作內容

**新增檔案**：
- `src/services/cacheStorage.js` — IndexedDB wrapper（getFromIDB / saveToIDB）
- `.github/workflows/daily-cache.yml` — 每日 cron（UTC 10:00 = 台北 18:00）+ workflow_dispatch 手動觸發

**修改檔案**：
- `src/services/api.js` — `fetchPrices()` 改為四層 fallback chain：
  ```
  ① 遠端 GitHub Pages URL (10s timeout)
  ② IndexedDB 本地快取
  ③ APK 內建靜態
  ④ 即時抓 4 天 (fallback)
  ```
  回傳簽章從 `Array` 改為 `{ data, lastUpdated, source }`
- `src/App.jsx` — 加 `cacheMeta` state + stale 提示 UI（>1 天顯示「已是 X 天前資料，建議連網更新」）
- `README.md` — 重寫部署流程段落

#### 公開 URL
`https://krownsh.github.io/vage-app/data/prices_cache.json`

### C. 本地驗證結果

#### ESM 模組載入
全部 exports ✓

#### fetchPrices 端對端測試 (Node 環境)
```
[fetchRemoteCache] 失敗: HTTP 404          ← GH Pages 還沒部署，預期
[IDB] 讀取失敗: indexedDB is not defined   ← Node 沒 IDB，預期
[fetchBuiltinCache] 失敗: Failed to parse URL ← Node 沒相對路徑，預期
[fetchPrices] fallback: 即時抓 4 天
source: 'live', lastUpdated: null, data.length: 2151
四層 fallback chain 全部按預期運作 ✓
```

### 已知小瑕疵
- `fetchPricesLive()` 抓的 raw data 還含 `交易量/種類代碼/市場代號`（slim 沒套用在 live fallback）。功能正確，但若未來要把 live 寫入 cache，需補 slim。

### 待後續
- [ ] 升級 Node 後 `npm run dev` 實測 App 行為
- [ ] 手動建 gh-pages 分支 + 推送 workflow
- [ ] 第一次手動觸發 workflow 確認部署成功
- [ ] App 內觀察 fetchPrices 是否成功從 GH Pages 拉到資料
- [ ] 補 MARKET_ALIAS 別名表（等實際 App 上線後看哪些品名 miss）
- [ ] 30 天價格曲線（DetailModal 已實作但要實機驗證）

---

## 下午追加 #6：GitHub Pages 部署驗證 + 髒資料修正

### 部署驗證結果（CLI 端）
```
HEAD: HTTP/2 200 ✓
Content-Type: application/json ✓
Content-Length: 1717195 (1.67 MB) ✓
Last-Modified: 2026-06-26 03:27:53 GMT ✓
Access-Control-Allow-Origin: * (CORS 開啟) ✓
```

### 發現的 data quality 問題
從 GH Pages 下載的 cache 內含 10 筆髒資料：
- 5 筆 `市場名稱` 是空字串 (`null`)
- 5 筆含 UTF-8 取代字元 `U+FFFD`（政府 API 部分字元編碼無法解碼，例：原應為「高雄市」變成「高雄市」）

影響：ProductCard / DetailModal 顯示空白市場卡片

### 修正
`scripts/daily_cache.cjs` 與 `src/services/api.js` 的 `applyFilters` 加市場名稱健全性檢查：
```js
r.市場名稱 && !r.市場名稱.includes('�')
```

### 本地重抓驗證
```
原始筆數: 47248
過濾後筆數: 10361
髒市場名稱筆數: 0  ← 修正生效
```

### 待使用者執行
1. `git add . && git commit -m "fix: 排除髒市場名稱資料"`
2. `git push origin main`
3. GitHub repo → Actions → Daily Cache Update → **Run workflow**（手動觸發重部署）
4. 等 1-2 分鐘後 `curl -I https://krownsh.github.io/vage-app/data/prices_cache.json` 確認 Last-Modified 更新

> GH Pages 上目前還是舊的含髒資料版本（上次 03:27 UTC 部署），需要重觸發 workflow 才會清掉。

---

## 下午追加 #7：UI/UX 逐項處理（依 待討論事項.md）

### D. 30 天趨勢改折線圖
- **新檔案** [src/components/TrendChart/TrendChart.jsx](../src/components/TrendChart/TrendChart.jsx) — 純 SVG 折線圖
- **特色**：Y 軸 5 刻度 + 標籤、X 軸 3 個日期標籤、平均虛線、最高/最低標註（紅/藍點）
- [src/components/DetailModal/DetailModal.jsx](../src/components/DetailModal/DetailModal.jsx) — 改用 TrendChart
- [src/index.css](../src/index.css) — `.trend-svg` 樣式

### A. ProductCard 主畫面重構（複習）
- aggregation 改用 mainName 當 key：428 cropName → 151 mainName（65% 縮減）
- ProductCard 移除 cheapest/fair/season/計算機；改 mainName 顯示；單一 avg 大數字
- DetailModal 加變體明細表

### #5 季節蔬果資料來源
- **研究**：農業知識入口網/農糧署/各地農會 — 都沒有完整開放資料 JSON
- **結論**：無現成 API
- **替代**：維持手寫 SEASON_MAP，標註來源 + 後續用 LLM 補完
- [src/utils/aggregation.js](../src/utils/aggregation.js) SEASON_MAP 加註解

### #1 賣場價格來源
- **研究**：全聯 PXGo / 家樂福 — 都沒有公開 API
- **結論**：短期不爬蟲（法律灰色地帶）
- **替代**：用「三市場實際行情高低差」取代「傳統/賣場/超市」假資料（已實作）

### #4 LLM 分析單顆重量（設計階段）
- **新檔案** [scripts/prompt_weight_estimation.md](../scripts/prompt_weight_estimation.md) — prompt 模板 + schema + 成本估計
- **新檔案** [scripts/estimate_crop_weights.cjs](../scripts/estimate_crop_weights.cjs) — batch 工具
  - `split`：919 筆 catalog 切成 14 個 batch（30 筆/批）
  - `merge`：合併 batches/responses/ 為 crop_weights.json
  - `status`：看進度
- **新檔案** [src/utils/cropWeights.js](../src/utils/cropWeights.js) — 前端載入 + 換算
  - `getCropWeight(cropName)` 取得重量資料
  - `convertPricePerKg(pricePerKg, cropName)` 換算成「元/顆」「元/束」
- **成本估計**：Claude 3.5 Sonnet 約 $0.30~0.50 USD

### #2 單位用克（設計階段）
- 價格保留「元/公斤」單位（合理）
- 顯示「單顆約 X 克 ≈ Y 元/顆」（需 crop_weights.json）
- 轉換工具已寫好（convertPricePerKg），等 weight 資料完成即可上 UI

### #3 30 天價格曲線
- ✅ 改折線圖完成（D 區）

---

## 下午追加 #8：NVIDIA NIM LLM 重量預估管線

### 環境

- 全域規則：[AGENT_GLOBAL_DIR/api/nvidia-nim.md](file) — NVIDIA NIM Hosted API 規格
- 本機 `.env`：`d:\others\sideproject\vage-app\.env`（10 把 NVIDIA API key，每把 40 RPM）
- 模型：`nvidia/llama-3.3-nemotron-super-49b-v1`（NVIDIA 優化版，中英文都強）
- Endpoint：`https://integrate.api.nvidia.com/v1/chat/completions`

### 工具更新

- [scripts/estimate_crop_weights.cjs](../scripts/estimate_crop_weights.cjs) — 新增 `run` / `test` 指令
  - `split`：919 筆 catalog 切成 14 batch
  - `test`：跑單一 batch 驗證 key + endpoint
  - `run`：round-robin 用 10 把 key 跑所有 batch（避開 40 RPM）
  - `merge`：合併 `batches/responses/` → `public/data/crop_weights.json`
  - `status`：看進度
- [scripts/prompt_weight_estimation.md](../scripts/prompt_weight_estimation.md) — 改成 NVIDIA NIM 設定 + 環境變數

### 模型選擇

| 候選 | 結果 |
|------|------|
| `meta/llama-3.1-8b-instruct` | ❌ 不穩定（echo user prompt、JSON 不完整、completion 4144 tokens 浪費） |
| `nvidia/llama-3.3-nemotron-super-49b-v1` | ✅ 完美（直接 JSON.parse 成功、completion 1705 tokens、會填 null） |

### 實測數據（batch_000 跑第一個 batch 30 項）

- 耗時：63 秒
- prompt tokens：858
- completion tokens：1705
- 解析成功率：30/30
- 範例輸出：
  ```json
  {
    "name": "人心果",
    "avgWeightKg": 0.15,
    "weightUnit": "perPiece",
    "confidence": "medium",
    "note": "平均直徑約5-6cm"
  }
  ```

### 接下來

- 跑全部 14 個 batch（背景執行中，預估 ~14 分鐘）
- merge 結果 → `public/data/crop_weights.json`
- 前端 [src/utils/cropWeights.js](../src/utils/cropWeights.js) 已寫好，等 crop_weights.json 出來即可用
- 之後：DetailModal 加「單顆約 X 克 ≈ Y 元」顯示（#2 收尾）

---

## 下午追加 #9：unit test + tool polishing

### A. unit test for aggregation.js

**新檔案** [scripts/test_aggregation.mjs](../scripts/test_aggregation.mjs) — 16 個測試案例

| 類別 | 測試數 |
|------|--------|
| aggregatePrices 主品項合併 | 8 |
| filterCrops 過濾與排序 | 5 |
| edge cases | 3 |

**覆蓋邏輯**：
- mainName 從 cropName 提取（玉米-白玉米 → 玉米）
- variants Set 去重
- markets 跨變體 + 跨日平均
- variantMarkets 明細
- 異常記錄過濾（空 cropName、價格 0）
- filterCrops: category (all/fav/fruit), query 大小寫不敏感, favorites, 排序

**結果**：16/16 通過

### B. 修復 weight run 失敗問題

**症狀**：7/14 batch 失敗，LLM 在 JSON 前加了「以下是根據提供的品項清單...」說明文字

**修法**：[scripts/estimate_crop_weights.cjs](../scripts/estimate_crop_weights.cjs) `extractJson()` 增強：
- 加 `matchAll` 處理多個 markdown code block
- 加 trailing comma 修復
- 加 `[ ... ]` 區段抓取（含說明文字）

**重跑**：npm run weights:run 會自動 skip 已成功的 batch

### C. package.json 整合

新增 12 個 npm scripts：
- `npm run test` — 跑 aggregation 測試
- `npm run catalog:update` — 更新 catalog
- `npm run cache:daily` — 跑每日 cache
- `npm run weights:split/test/run/merge/status` — LLM 重量預估
- `npm run seasons:split/run/merge` — LLM 季節預估

### D. 進度

- weight batch：8/14 完成（首次跑），6 個失敗 batch 重跑中
- season batch：14 個已切好，等 weight 跑完接力
- merge → crop_weights.json → DetailModal 單顆重量 UI 自動生效

---

## 下午追加 #10：weight batch 完成 + UI 自動生效

### weight batch 最終結果

| 狀態 | 數量 |
|------|------|
| 成功 | 12 / 14 |
| 失敗（extractJson 沒修好前跑） | 2 (batch_005, 013) |
| **合併後有效** | **251 筆** |
| Catalog 變體覆蓋率 | 27%（251 / 918） |

### LLM 模型最終選擇

`nvidia/llama-3.3-nemotron-super-49b-v1`（NVIDIA 優化版，中英文都強）

### Schema 意外發現

**LLM 對有多變體的品項會把 `avgWeightKg` 變成 nested object**：

```json
{
  "name": "柚子",
  "avgWeightKg": {
    "柚子-蜜柚": 0.6,
    "柚子-文旦": 0.7,
    ...
  }
}
```

**處理**：merge 階段自動攤平（取所有非 null 變體的平均），寫入 note 解釋。

### crop_weights.json 驗證

```
信心度: high 60 / medium 169 / low 22
重量: 最小 0.005kg, 中位數 0.15kg, 最大 6kg
```

### 新增指令

- `npm run weights:validate` — 驗證合併結果分佈
- `npm run weights:gap` — 列出 catalog 缺漏（202 mainName / 667 變體）
- `npm run seasons:validate` — 同樣的驗證給季節用

### DetailModal 單顆重量 UI

- ✅ 已寫入 [src/components/DetailModal/DetailModal.jsx](src/components/DetailModal/DetailModal.jsx)
- ✅ 自動讀取 crop_weights.json，沒資料時隱藏
- ✅ 玉米 ≈ 300g, 椰子 ≈ 1.2kg 之類合理值會顯示

### season batch 接力

- 背景跑中（2/14）
- 預計 18 分鐘跑完
- 完成後 `npm run seasons:merge` → `crop_seasons.json`
- 之後可在 DetailModal 加「產季中」標籤

---

## 下午追加 #11：parallel run + season UI

### 改進：run 改為 parallel

**問題**：之前 round-robin 一個一個跑，14 batch × 90s = 21 分鐘
**修正**：10 把 key 同時打，14 batch parallel，max batch time = ~4 分鐘

| 階段 | 舊（sequential） | 新（parallel） |
|------|---------------|---------------|
| weight 14 batch | ~21 分鐘 | ~5 分鐘 |
| season 14 batch | ~21 分鐘 | ~5 分鐘 |

### 最終統計

| 檔案 | 筆數 | Catalog 覆蓋率 | 信心度 high |
|------|------|----------------|------------|
| `crop_weights.json` | **270** | 29.4% | 67 (25%) |
| `crop_seasons.json` | **369** | 40.2% | 148 (40%) |
| 當月（6月）產季 | 144 筆 | - | - |

### DetailModal 新功能

1. **單顆重量提示**：`單顆約 300 克 ≈ 12 元/條`（玉米）
2. **當季標籤**：當季 / 盛產 / 非當季（用 LLM 補完真實資料取代手寫 SEASON_MAP）
3. **兩者 confidence='low' 時**顯示「（參考值）」提示

### 新增檔案

- `public/data/crop_weights.json` (270 筆)
- `public/data/crop_weights_gap.json` (667 變體缺漏)
- `public/data/crop_seasons.json` (369 筆)
- `scripts/_extract_json.mjs` (提取 JSON helper，cjs + mjs 共用)
- `scripts/test_extract_json.mjs` (10 個 test)

### 修改檔案

- `src/components/DetailModal/DetailModal.jsx` — 單顆重量 + 當季標籤
- `src/utils/cropSeasons.js` — 新建
- `scripts/estimate_crop_weights.cjs` — parallel run、merge 攤平 nested object、validate、gap 指令
- `scripts/estimate_crop_seasons.cjs` — parallel run、validate 指令
- `scripts/_extract_json.mjs` — 抽出 ESM helper
- `package.json` — 14 個 npm scripts（test + weights:* + seasons:*）
- `README.md` — 完整 NVIDIA NIM 章節 + parallel 說明
- `docs/architecture_map.md` — 加入 LLM 流程

---

## 晚間追加 #12：UI/UX 全面對齊 [docs/uiux.md](../docs/uiux.md)

### 動機
主人提供 [docs/uiux.md](../docs/uiux.md)（v1.0，23 章規格），並附 [docs/01.png](../docs/01.png)（列表頁）與 [docs/02.png](../docs/02.png)（詳情 Bottom Sheet）兩張視覺基準。原先的綠底表格風 UI 與規格落差大，這次全面改版。

### 決策拍板（已與主人確認）
- **地區 chips**：移除（規格列為核心，但主人決定暫不需要定位功能）
- **圖片佔位**：「奶油底圓 + 中文首字」→ 統一由 [src/components/CropIcon/CropIcon.jsx](../src/components/CropIcon/CropIcon.jsx) 元件管理，主人日後給圖只改這個檔
- **字型**：Noto Sans TC 全字重（400-900），從 Google Fonts import

### 完成內容

#### A. Design Tokens（[src/index.css](../src/index.css) 全重寫）
- 新增 CSS variables：`--bg-base #FFF8E8` / `--card-warm #FFFBF1` / `--yellow #FFC51B` / `--green #236B34` / 分類 tag 顏色（綠 / 根莖棕 / 花菜橘 / 未分類紫）
- 圓角：`--radius-lg 22px` / `--radius-sheet 36px` / `--radius-pill 999px`
- 陰影：`--shadow-card` / `--shadow-fab` / `--shadow-sheet`
- 全域 reset：font-family `Noto Sans TC, PingFang TC, Microsoft JhengHei, sans-serif`
- 新增字型載入：`@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;500;600;700;800;900&display=swap')`

#### B. 元件重構
| 元件 | 改動重點 |
|------|----------|
| [src/components/Header/Header.jsx](../src/components/Header/Header.jsx) | 移除 `market-chips`、加入 36px/900 大標題、農業 badge（綠底）、右上插圖佔位 |
| [src/components/CategoryTabs/CategoryTabs.jsx](../src/components/CategoryTabs/CategoryTabs.jsx) | 64px 白圓容器 + 黃底 chip + `data-cat` 屬性驅動分類色 |
| [src/components/ProductCard/ProductCard.jsx](../src/components/ProductCard/ProductCard.jsx) | 重排版：左 CropIcon → 名稱 + 分類 tag + 大價 + 單位 → 右側 fav + share |
| [src/components/FAB/FAB.jsx](../src/components/FAB/FAB.jsx) | 改為純加號 icon（黃圓黑邊由 CSS 控制） |
| [src/components/DetailModal/DetailModal.jsx](../src/components/DetailModal/DetailModal.jsx) | 完整 Bottom Sheet 結構：drag handle + 關閉鈕 + summary card（漸層）+ 市場/變體表格 + 輸入區 |
| [src/components/TrendChart/TrendChart.jsx](../src/components/TrendChart/TrendChart.jsx) | 對齊 §16：綠線 #168A3A / 高點黃圓 + 「高 $X.X」紅標 / 虛線平均線 + 右側「均」 |
| [src/components/CheckerModal/CheckerModal.jsx](../src/components/CheckerModal/CheckerModal.jsx) | 沿用 Bottom Sheet 結構 + 黃色按鈕 |
| **NEW** [src/components/CropIcon/CropIcon.jsx](../src/components/CropIcon/CropIcon.jsx) | 統一圖片佔位元件（68×68 卡片 / 64×64 summary） |

#### C. App 與進入點
- [src/App.jsx](../src/App.jsx) — `cache-meta` 加綠色時鐘 SVG icon
- [index.html](../index.html) — `theme-color` 改為奶油色 `#FFF8E8`、加 Google Fonts preconnect

### 設計規格對齊驗收

| 規格項 | 狀態 |
|--------|------|
| 背景 `#FFF8E8` 非純白 | ✅ |
| 大標 36px/900、letter-spacing -0.5px | ✅ |
| 卡片高度 ≥ 82px（用 86px min-height） | ✅ |
| 價格 34px/900、單位 15px/500 | ✅ |
| 分類 tag 顏色：葉菜綠 / 根莖棕 / 花菜橘 / 未分類紫 | ✅（data-cat 切換） |
| Bottom Sheet 36px 上緣圓角 | ✅ |
| Sheet drag handle 54×5 灰 | ✅ |
| 關閉鈕 48×48 圓 `#F3EAD9` | ✅ |
| Summary card 104px 漸層 | ✅ |
| 市場表 row 48px、變體表 row 44px | ✅ |
| FAB 68×68 黃圓黑邊 | ✅ |
| TrendChart 綠線 3px / 高點 r=7 黃 / 平均虛線 | ✅ |
| 收藏 active `#FF6B4A` + pop 動畫 | ✅ |

### 架構圖更新
- [docs/architecture_map.md](../docs/architecture_map.md) — 加入 UI 元件層（CropIcon / Header / CategoryTabs / ProductCard / DetailModal / TrendChart / FAB / CheckerModal）

### 檔案變動
- [MODIFY] [src/index.css](../src/index.css) — 全重寫
- [MODIFY] [src/components/Header/Header.jsx](../src/components/Header/Header.jsx) — 移除地區 chips
- [MODIFY] [src/components/CategoryTabs/CategoryTabs.jsx](../src/components/CategoryTabs/CategoryTabs.jsx) — 樣式由 CSS 控
- [MODIFY] [src/components/ProductCard/ProductCard.jsx](../src/components/ProductCard/ProductCard.jsx) — 新版 layout
- [MODIFY] [src/components/FAB/FAB.jsx](../src/components/FAB/FAB.jsx) — 純加號
- [MODIFY] [src/components/DetailModal/DetailModal.jsx](../src/components/DetailModal/DetailModal.jsx) — Bottom Sheet 結構
- [MODIFY] [src/components/TrendChart/TrendChart.jsx](../src/components/TrendChart/TrendChart.jsx) — 對齊 §16
- [MODIFY] [src/components/CheckerModal/CheckerModal.jsx](../src/components/CheckerModal/CheckerModal.jsx) — 沿用 sheet 結構
- [MODIFY] [src/App.jsx](../src/App.jsx) — cache-meta icon
- [MODIFY] [index.html](../index.html) — theme color + preconnect
- [NEW]    [src/components/CropIcon/CropIcon.jsx](../src/components/CropIcon/CropIcon.jsx) — 統一圖片佔位

### 待主人提供
- [ ] 各作物手繪插圖 PNG/SVG（之後只需改 CropIcon.jsx）
- [ ] 右上蔬菜籃插圖（之後只需改 Header.jsx 的 `.header-illustration` 內容）

### 驗證
- ✅ `npm run build`：32 modules / 2.03s / 0 error / gzip CSS 3.02 KB / JS 68.57 KB
- ✅ `npm test`：16 / 16 aggregation 邏輯測試通過（搜尋 / 分類 / 收藏 / 主品項合併 / 跨變體平均 全部保留）
- ⏳ 視覺：主人實際開瀏覽器對照 [docs/01.png](../docs/01.png) / [docs/02.png](../docs/02.png)
- ⏳ 實機：搜尋 / 分類 / 收藏 / 詳情 / 30 天趨勢 / 季節標 / 重量提示 / 划算計算機（程式碼保留，需在瀏覽器或實機驗證）
